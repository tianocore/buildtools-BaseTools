#DEPFILES = $(OBJECTS:%.o=%.d)

%.obj : %.c
	$(CC) -c $(CFLAGS) $< -o $@

%.obj : %.S
	$(AS) -c $(ASFLAGS) $< -o $@

$(LIBRARY) : $(OBJECTS)
	-@if not exist $(LIB_PATH) mkdir $(LIB_PATH)
	$(AR) /nologo /out:$@ $?

#-include $(DEPFILES)

